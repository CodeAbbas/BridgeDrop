import BridgeDrop from '@/components/BridgeDrop';

export const metadata = {
  title: 'BridgeDrop | Liquid Glass Transfer',
  description: 'Secure P2P file transfer for iOS 12 and Android.',
};

export default function Home() {
  return (
    <main>
      <BridgeDrop />
    </main>
  );
}
